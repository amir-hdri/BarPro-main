diff --git a/.github/workflows/cd-deploy.yml b/.github/workflows/cd-deploy.yml
index e4533ef..99b5c13 100644
--- a/.github/workflows/cd-deploy.yml
+++ b/.github/workflows/cd-deploy.yml
@@ -25,6 +25,11 @@ concurrency:
   group: ${{ github.workflow }}-${{ github.ref }}
   cancel-in-progress: true
 
+# Required for GITHUB_TOKEN to publish backend/frontend images to GHCR.
+permissions:
+  contents: read
+  packages: write
+
 env:
   PYTHON_VERSION: "3.11"
   NODE_VERSION: "20"
@@ -42,13 +47,13 @@ jobs:
     
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Docker Buildx
-        uses: docker/setup-buildx-action@v3
+        uses: docker/setup-buildx-action@v4
 
       - name: Login to Container Registry
-        uses: docker/login-action@v3
+        uses: docker/login-action@v4
         with:
           registry: ${{ env.REGISTRY }}
           username: ${{ github.actor }}
@@ -56,7 +61,7 @@ jobs:
 
       - name: Extract Metadata
         id: meta
-        uses: docker/metadata-action@v5
+        uses: docker/metadata-action@v6
         with:
           images: ${{ env.REGISTRY }}/${{ github.repository }}/${{ env.IMAGE_NAME }}-backend
           tags: |
@@ -64,11 +69,11 @@ jobs:
             type=ref,event=tag
             type=ref,event=pr
             type=sha,prefix={{branch}}-,priority=100
-            type=raw,value=latest,enable={{is_default_branch}}
+            type=raw,value=latest
 
       - name: Build and Push Backend Image
         id: build
-        uses: docker/build-push-action@v5
+        uses: docker/build-push-action@v7
         with:
           context: .
           file: Dockerfile
@@ -88,10 +93,10 @@ jobs:
     
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Node.js
-        uses: actions/setup-node@v4
+        uses: actions/setup-node@v7
         with:
           node-version: ${{ env.NODE_VERSION }}
           cache: 'npm'
@@ -108,10 +113,10 @@ jobs:
           NEXT_PUBLIC_API_URL="/api" npm run build
 
       - name: Set up Docker Buildx
-        uses: docker/setup-buildx-action@v3
+        uses: docker/setup-buildx-action@v4
 
       - name: Login to Container Registry
-        uses: docker/login-action@v3
+        uses: docker/login-action@v4
         with:
           registry: ${{ env.REGISTRY }}
           username: ${{ github.actor }}
@@ -119,7 +124,7 @@ jobs:
 
       - name: Extract Metadata
         id: meta
-        uses: docker/metadata-action@v5
+        uses: docker/metadata-action@v6
         with:
           images: ${{ env.REGISTRY }}/${{ github.repository }}/${{ env.IMAGE_NAME }}-frontend
           tags: |
@@ -127,11 +132,11 @@ jobs:
             type=ref,event=tag
             type=ref,event=pr
             type=sha,prefix={{branch}}-,priority=100
-            type=raw,value=latest,enable={{is_default_branch}}
+            type=raw,value=latest
 
       - name: Build and Push Frontend Image
         id: build
-        uses: docker/build-push-action@v5
+        uses: docker/build-push-action@v7
         with:
           context: ./apps/web
           file: ./apps/web/Dockerfile
@@ -145,7 +150,7 @@ jobs:
     name: Deploy to Staging
     runs-on: ubuntu-latest
     needs: [build-backend, build-frontend]
-    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.environment == 'staging')
+    if: (github.event_name == 'push' && github.ref == 'refs/heads/main') || (github.event_name == 'workflow_dispatch' && inputs.environment == 'staging')
     
     environment:
       name: staging
@@ -153,7 +158,7 @@ jobs:
     
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Deploy to Staging Server
         uses: appleboy/ssh-action@v1
@@ -163,22 +168,22 @@ jobs:
           key: ${{ secrets.STAGING_SSH_KEY }}
           port: ${{ secrets.STAGING_PORT || 22 }}
           script: |
-            # Pull latest images
-            docker-compose -f /opt/barpro/docker-compose.yml pull
-            
-            # Stop services
-            docker-compose -f /opt/barpro/docker-compose.yml down
-            
-            # Start with new images
-            docker-compose -f /opt/barpro/docker-compose.yml up -d
-            
-            # Run database migrations
-            docker-compose -f /opt/barpro/docker-compose.yml exec backend alembic upgrade head
-            
-            # Health check
+            set -e
+            cd /opt/barpro
+            git checkout main
+            git pull --ff-only origin main
+            export BACKEND_IMAGE="ghcr.io/amir-hdri/barpro-main/barpro-backend:latest"
+            export FRONTEND_IMAGE="ghcr.io/amir-hdri/barpro-main/barpro-frontend:latest"
+            echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
+
+            docker compose pull
+            docker compose down
+            docker compose up -d
+            docker compose exec -T backend alembic upgrade head
+
             sleep 10
-            docker-compose -f /opt/barpro/docker-compose.yml ps
-            curl -f http://localhost:8000/healthz || true
+            docker compose ps
+            curl -f http://localhost:8000/healthz
 
   deploy-production:
     name: Deploy to Production
@@ -192,7 +197,7 @@ jobs:
     
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Wait for Manual Approval
         if: github.event_name != 'workflow_dispatch'
@@ -206,33 +211,24 @@ jobs:
           key: ${{ secrets.PRODUCTION_SSH_KEY }}
           port: ${{ secrets.PRODUCTION_PORT || 22 }}
           script: |
-            # Backup current deployment
+            set -e
             cd /opt/barpro
-            git add .
-            git commit -m "Pre-deploy backup $(date)" || true
-            
-            # Pull latest images
-            docker-compose -f docker-compose.yml pull
-            
-            # Stop services gracefully
-            docker-compose -f docker-compose.yml down
-            
-            # Start with new images
-            docker-compose -f docker-compose.yml up -d
-            
-            # Run database migrations
-            docker-compose -f docker-compose.yml exec backend alembic upgrade head
-            
-            # Clear caches
-            # docker-compose -f docker-compose.yml exec redis redis-cli FLUSHALL || true
-            # FLUSHALL disabled: clearing all Redis data during deployment destroys active sessions and task state.
-            
-            # Health check
+            git fetch origin --tags
+            git checkout --detach "${{ github.sha }}"
+            export BACKEND_IMAGE="ghcr.io/amir-hdri/barpro-main/barpro-backend:latest"
+            export FRONTEND_IMAGE="ghcr.io/amir-hdri/barpro-main/barpro-frontend:latest"
+            echo "${{ github.token }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin
+
+            docker compose pull
+            docker compose down
+            docker compose up -d
+            docker compose exec -T backend alembic upgrade head
+
+            # FLUSHALL intentionally disabled: it would destroy active sessions
+            # and task state during deployment.
             sleep 15
-            docker-compose -f docker-compose.yml ps
-            curl -f https://barpro.com/healthz || true
-            
-            # Notify success
+            docker compose ps
+            curl -f https://barpro.com/healthz
             echo "Production deployment completed successfully"
 
   notify:
diff --git a/.github/workflows/ci-cd.yml b/.github/workflows/ci-cd.yml
index 52b0901..d4c3b3a 100644
--- a/.github/workflows/ci-cd.yml
+++ b/.github/workflows/ci-cd.yml
@@ -1,4 +1,4 @@
-name: CI/CD - Complete Pipeline
+name: CI - Complete Pipeline
 
 on:
   push:
@@ -10,13 +10,18 @@ concurrency:
   group: ${{ github.workflow }}-${{ github.ref }}
   cancel-in-progress: true
 
+permissions:
+  contents: read
+
 env:
   PYTHON_VERSION: "3.11"
   NODE_VERSION: "20"
+  ENVIRONMENT: "test"
   HEADLESS: "true"
-  MASTER_ADMIN_PASSWORD: ${{ secrets.MASTER_ADMIN_PASSWORD }}
-  JWT_SECRET: ${{ secrets.JWT_SECRET }}
-  DRIVER_ENCRYPTION_KEY: ${{ secrets.DRIVER_ENCRYPTION_KEY }}
+  # Deterministic, non-production credentials used only by CI processes.
+  MASTER_ADMIN_PASSWORD: '$2b$12$a3tOM8qQ30A0ecM/cF4HvObV/UTRknpu6VqcfDzEEhHGfKeBiaFEu'
+  JWT_SECRET: "ci-only-jwt-secret-not-for-production-32-chars"
+  DRIVER_ENCRYPTION_KEY: "CQVgdhGECQA3DfLOcuHbmFI-IzFspWiCkVfiz3NkUS4="
 
 jobs:
   quality-checks:
@@ -25,16 +30,16 @@ jobs:
     
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@v7
         with:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
 
       - name: Set up Node.js
-        uses: actions/setup-node@v4
+        uses: actions/setup-node@v7
         with:
           node-version: ${{ env.NODE_VERSION }}
           cache: 'npm'
@@ -42,28 +47,35 @@ jobs:
 
       - name: Install Dependencies
         run: |
+          python -m pip install --upgrade pip==26.2.1
           pip install -r requirements-dev.txt
           cd apps/web && npm ci
 
-      - name: Run Linting
-        run: |
-          ruff check app/ tests/
-          black --check app/ tests/
-          cd apps/web && npm run lint
+      - name: Run Backend Linting
+        run: ruff check app/ tests/
 
-      - name: Run Type Checking
-        run: |
-          mypy app/ --ignore-missing-imports
-          cd apps/web && npx tsc --noEmit
+      - name: Check Backend Formatting
+        run: black --check app/ tests/
 
-      - name: Check Formatting
-        run: |
-          ruff format --check app/ tests/
+      - name: Run Frontend Linting
+        working-directory: apps/web
+        run: npm run lint
+
+      - name: Audit Frontend Dependencies
+        working-directory: apps/web
+        run: npm audit --audit-level=moderate
+
+      - name: Run Backend Type Checking
+        run: mypy app/ --ignore-missing-imports
+
+      - name: Run Frontend Type Checking
+        working-directory: apps/web
+        run: npx tsc --noEmit
 
   test:
     name: Run Tests
     runs-on: ubuntu-latest
-    needs: quality-checks
+    # Run independently so a quality failure cannot hide the test signal.
     
     services:
       redis:
@@ -81,16 +93,17 @@ jobs:
 
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@v7
         with:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
 
       - name: Install Dependencies
         run: |
+          python -m pip install --upgrade pip==26.2.1
           pip install -r requirements.txt
           pip install -r requirements-dev.txt
 
@@ -100,36 +113,33 @@ jobs:
 
       - name: Run Unit Tests
         run: |
-          pytest tests/ -v --tb=short --cov=app --cov-report=term-missing
+          pytest tests/ -v --tb=short -W error --cov=app --cov-report=term-missing --cov-report=xml --cov-report=html
         env:
           DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/utcms_rpa_test
           REDIS_URL: redis://localhost:6379/0
-          JWT_SECRET: ${{ secrets.JWT_SECRET }}
-          DRIVER_ENCRYPTION_KEY: ${{ secrets.DRIVER_ENCRYPTION_KEY }}
 
       - name: Upload Coverage
-        uses: actions/upload-artifact@v4
+        if: always()
+        uses: actions/upload-artifact@v7
         with:
           name: test-coverage
-          path: htmlcov/
+          path: |
+            coverage.xml
+            htmlcov/
           retention-days: 7
 
   build:
     name: Build Docker Images
     runs-on: ubuntu-latest
-    needs: test
+    needs: [quality-checks, test]
     if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v')
     
-    outputs:
-      backend_image: ${{ steps.build-backend.outputs.image }}
-      frontend_image: ${{ steps.build-frontend.outputs.image }}
-    
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Node.js
-        uses: actions/setup-node@v4
+        uses: actions/setup-node@v7
         with:
           node-version: ${{ env.NODE_VERSION }}
           cache: 'npm'
@@ -146,91 +156,26 @@ jobs:
           NEXT_PUBLIC_API_URL="/api" npm run build
 
       - name: Set up Docker Buildx
-        uses: docker/setup-buildx-action@v3
-
-      - name: Login to Docker Hub
-        if: github.event_name != 'pull_request'
-        uses: docker/login-action@v3
-        with:
-          username: ${{ secrets.DOCKER_USERNAME }}
-          password: ${{ secrets.DOCKER_PASSWORD }}
+        uses: docker/setup-buildx-action@v4
 
       - name: Build Backend Image
         id: build-backend
-        uses: docker/build-push-action@v5
+        uses: docker/build-push-action@v7
         with:
           context: .
           file: Dockerfile
-          push: ${{ github.event_name != 'pull_request' }}
-          tags: ${{ github.repository }}-backend:latest,${{ github.repository }}-backend:${{ github.sha }}
+          push: false
+          tags: barpro-backend:ci-${{ github.sha }}
           cache-from: type=gha
           cache-to: type=gha,mode=max
 
       - name: Build Frontend Image
         id: build-frontend
-        uses: docker/build-push-action@v5
+        uses: docker/build-push-action@v7
         with:
           context: ./apps/web
           file: ./apps/web/Dockerfile
-          push: ${{ github.event_name != 'pull_request' }}
-          tags: ${{ github.repository }}-frontend:latest,${{ github.repository }}-frontend:${{ github.sha }}
+          push: false
+          tags: barpro-frontend:ci-${{ github.sha }}
           cache-from: type=gha
           cache-to: type=gha,mode=max
-
-  deploy-staging:
-    name: Deploy to Staging
-    runs-on: ubuntu-latest
-    needs: build
-    if: github.ref == 'refs/heads/main' && github.event_name != 'pull_request'
-    
-    environment:
-      name: staging
-      url: https://staging.${{ github.repository_owner}}.com
-    
-    steps:
-      - name: Checkout Repository
-        uses: actions/checkout@v4
-
-      - name: Deploy via SSH
-        uses: appleboy/ssh-action@v1
-        with:
-          host: ${{ secrets.STAGING_HOST }}
-          username: ${{ secrets.STAGING_USERNAME }}
-          key: ${{ secrets.STAGING_SSH_KEY }}
-          script: |
-            cd /opt/${{ github.repository }}
-            git pull origin main
-            docker-compose pull
-            docker-compose down
-            docker-compose up -d
-            docker-compose exec backend alembic upgrade head
-
-  load-test:
-    name: Load Testing
-    runs-on: ubuntu-latest
-    needs: deploy-staging
-    if: github.ref == 'refs/heads/main'
-    
-    steps:
-      - name: Checkout Repository
-        uses: actions/checkout@v4
-
-      - name: Set up Python
-        uses: actions/setup-python@v5
-        with:
-          python-version: ${{ env.PYTHON_VERSION }}
-
-      - name: Install Locust
-        run: pip install locust
-
-      - name: Run Load Test
-        run: |
-          locust -f tests/load/locustfile.py --host=https://staging.${{ github.repository_owner}}.com --headless -u 100 -r 10 --run-time 5m --html=load-test-report.html
-        continue-on-error: true
-
-      - name: Upload Load Test Report
-        uses: actions/upload-artifact@v4
-        with:
-          name: load-test-report
-          path: load-test-report.html
-          retention-days: 30
diff --git a/.github/workflows/ci-test.yml b/.github/workflows/ci-test.yml
index 393e734..11b8260 100644
--- a/.github/workflows/ci-test.yml
+++ b/.github/workflows/ci-test.yml
@@ -16,11 +16,41 @@ env:
   DATABASE_URL: "sqlite+aiosqlite:///./test.db"
   ENVIRONMENT: "test"
   HEADLESS: "true"
-  MASTER_ADMIN_PASSWORD: ${{ secrets.MASTER_ADMIN_PASSWORD }}
-  JWT_SECRET: ${{ secrets.JWT_SECRET }}
-  DRIVER_ENCRYPTION_KEY: ${{ secrets.DRIVER_ENCRYPTION_KEY }}
+  # Deterministic, non-production credentials used only by CI processes.
+  MASTER_ADMIN_PASSWORD: '$2b$12$a3tOM8qQ30A0ecM/cF4HvObV/UTRknpu6VqcfDzEEhHGfKeBiaFEu'
+  JWT_SECRET: "ci-only-jwt-secret-not-for-production-32-chars"
+  DRIVER_ENCRYPTION_KEY: "CQVgdhGECQA3DfLOcuHbmFI-IzFspWiCkVfiz3NkUS4="
 
 jobs:
+  backend-quality:
+    name: Backend Quality
+    runs-on: ubuntu-latest
+
+    steps:
+      - name: Checkout Repository
+        uses: actions/checkout@v7
+
+      - name: Set up Python
+        uses: actions/setup-python@v7
+        with:
+          python-version: ${{ env.PYTHON_VERSION }}
+          cache: 'pip'
+          cache-dependency-path: 'requirements-dev.txt'
+
+      - name: Install Quality Tools
+        run: |
+          python -m pip install --upgrade pip==26.2.1
+          pip install -r requirements-dev.txt
+
+      - name: Run Ruff
+        run: ruff check app/ tests/
+
+      - name: Check Black Formatting
+        run: black --check app/ tests/
+
+      - name: Run Mypy
+        run: mypy app/ --ignore-missing-imports
+
   backend-test:
     name: Backend Tests
     runs-on: ubuntu-latest
@@ -51,10 +81,10 @@ jobs:
 
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@v7
         with:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
@@ -62,7 +92,7 @@ jobs:
 
       - name: Install Python Dependencies
         run: |
-          python -m pip install --upgrade pip
+          python -m pip install --upgrade pip==26.2.1
           pip install -r requirements.txt
           pip install -r requirements-dev.txt
 
@@ -71,15 +101,9 @@ jobs:
           pip install playwright
           playwright install chromium --with-deps
 
-      - name: Run Backend Linting
-        run: |
-          ruff check app/ tests/
-          black --check app/ tests/
-          mypy app/ --ignore-missing-imports
-
       - name: Run Backend Unit Tests
         run: |
-          pytest tests/ -v --tb=short --cov=app --cov-report=term-missing --cov-report=xml
+          pytest tests/ -v --tb=short -W error --cov=app --cov-report=term-missing --cov-report=xml --cov-report=html
           coverage xml -o coverage.xml
         env:
           DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/utcms_rpa_test
@@ -89,7 +113,7 @@ jobs:
 
       - name: Upload Coverage Report
         if: always()
-        uses: actions/upload-artifact@v4
+        uses: actions/upload-artifact@v7
         with:
           name: backend-coverage
           path: |
@@ -103,10 +127,10 @@ jobs:
     
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Node.js
-        uses: actions/setup-node@v4
+        uses: actions/setup-node@v7
         with:
           node-version: ${{ env.NODE_VERSION }}
           cache: 'npm'
@@ -119,8 +143,11 @@ jobs:
 
       - name: Run Frontend Linting
         working-directory: apps/web
-        run: |
-          npm run lint
+        run: npm run lint
+
+      - name: Audit Frontend Dependencies
+        working-directory: apps/web
+        run: npm audit --audit-level=moderate
 
       - name: Run Frontend Type Checking
         working-directory: apps/web
@@ -153,22 +180,23 @@ jobs:
 
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@v7
         with:
           python-version: ${{ env.PYTHON_VERSION }}
           cache: 'pip'
 
       - name: Install Dependencies
         run: |
+          python -m pip install --upgrade pip==26.2.1
           pip install -r requirements.txt
           pip install -r requirements-dev.txt
 
       - name: Run Integration Tests
         run: |
-          pytest tests/test_integration/ -v --tb=short
+          pytest tests/test_integration/ -v --tb=short -W error
         env:
           DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/utcms_rpa_test
           REDIS_URL: redis://localhost:6379/0
@@ -182,27 +210,25 @@ jobs:
     
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Set up Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@v7
         with:
           python-version: ${{ env.PYTHON_VERSION }}
 
       - name: Install Security Tools
         run: |
-          pip install bandit safety pip-audit
+          python -m pip install --upgrade pip==26.2.1
+          pip install bandit==1.9.4 safety==3.8.1 pip-audit==2.10.1
 
       - name: Run Bandit Security Linter
         run: |
           bandit -r app/ -ll -ii
 
       - name: Run Safety Check
-        run: |
-          safety check --full-report
-          continue-on-error: true
+        continue-on-error: true
+        run: safety check --full-report -r requirements.txt
 
-      - name: Audit Dependencies
-        run: |
-          pip-audit --desc --strict
-          continue-on-error: true
+      - name: Audit Runtime Dependencies
+        run: pip-audit -r requirements.txt --desc --strict
diff --git a/.github/workflows/dependency-submission.yml b/.github/workflows/dependency-submission.yml
index 0b20b02..a949813 100644
--- a/.github/workflows/dependency-submission.yml
+++ b/.github/workflows/dependency-submission.yml
@@ -19,18 +19,18 @@ jobs:
       contents: write
     steps:
       - name: Checkout Repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@v7
 
       - name: Setup Python
-        uses: actions/setup-python@v5
+        uses: actions/setup-python@v7
         with:
           python-version: '3.11'
           cache: 'pip'
 
       - name: Install Dependencies
         run: |
-          python -m pip install --upgrade pip
-          pip install pip-audit
+          python -m pip install --upgrade pip==26.2.1
+          pip install pip-audit==2.10.1
 
       - name: Generate Dependency Snapshot
         run: |
@@ -38,7 +38,7 @@ jobs:
           cat dependencies.json
 
       - name: Upload Dependency Snapshot
-        uses: actions/upload-artifact@v4
+        uses: actions/upload-artifact@v7
         with:
           name: dependencies
           path: dependencies.json
@@ -56,7 +56,7 @@ jobs:
         continue-on-error: true
 
       - name: Upload SBOM
-        uses: actions/upload-artifact@v4
+        uses: actions/upload-artifact@v7
         with:
           name: sbom
           path: cyclonedx-bom.xml
