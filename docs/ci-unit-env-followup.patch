diff --git a/.github/workflows/ci-cd.yml b/.github/workflows/ci-cd.yml
index d4c3b3a..436b569 100644
--- a/.github/workflows/ci-cd.yml
+++ b/.github/workflows/ci-cd.yml
@@ -16,6 +16,8 @@ permissions:
 env:
   PYTHON_VERSION: "3.11"
   NODE_VERSION: "20"
+  DATABASE_URL: "sqlite+aiosqlite:///./test.db"
+  REDIS_URL: "redis://localhost:6379/0"
   ENVIRONMENT: "test"
   HEADLESS: "true"
   # Deterministic, non-production credentials used only by CI processes.
@@ -76,20 +78,6 @@ jobs:
     name: Run Tests
     runs-on: ubuntu-latest
     # Run independently so a quality failure cannot hide the test signal.
-    
-    services:
-      redis:
-        image: redis:7
-        ports:
-          - 6379:6379
-      postgres:
-        image: postgres:16
-        env:
-          POSTGRES_USER: postgres
-          POSTGRES_PASSWORD: postgres
-          POSTGRES_DB: utcms_rpa_test
-        ports:
-          - 5432:5432
 
     steps:
       - name: Checkout Repository
@@ -114,9 +102,6 @@ jobs:
       - name: Run Unit Tests
         run: |
           pytest tests/ -v --tb=short -W error --cov=app --cov-report=term-missing --cov-report=xml --cov-report=html
-        env:
-          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/utcms_rpa_test
-          REDIS_URL: redis://localhost:6379/0
 
       - name: Upload Coverage
         if: always()
diff --git a/.github/workflows/ci-test.yml b/.github/workflows/ci-test.yml
index 11b8260..c6761e8 100644
--- a/.github/workflows/ci-test.yml
+++ b/.github/workflows/ci-test.yml
@@ -14,6 +14,7 @@ env:
   PYTHON_VERSION: "3.11"
   NODE_VERSION: "20"
   DATABASE_URL: "sqlite+aiosqlite:///./test.db"
+  REDIS_URL: "redis://localhost:6379/0"
   ENVIRONMENT: "test"
   HEADLESS: "true"
   # Deterministic, non-production credentials used only by CI processes.
@@ -54,30 +55,6 @@ jobs:
   backend-test:
     name: Backend Tests
     runs-on: ubuntu-latest
-    
-    services:
-      redis:
-        image: redis:7
-        ports:
-          - 6379:6379
-        options: >-
-          --health-cmd "redis-cli ping"
-          --health-interval 10s
-          --health-timeout 5s
-          --health-retries 5
-      postgres:
-        image: postgres:16
-        env:
-          POSTGRES_USER: postgres
-          POSTGRES_PASSWORD: postgres
-          POSTGRES_DB: utcms_rpa_test
-        ports:
-          - 5432:5432
-        options: >-
-          --health-cmd pg_isready
-          --health-interval 10s
-          --health-timeout 5s
-          --health-retries 5
 
     steps:
       - name: Checkout Repository
@@ -105,11 +82,6 @@ jobs:
         run: |
           pytest tests/ -v --tb=short -W error --cov=app --cov-report=term-missing --cov-report=xml --cov-report=html
           coverage xml -o coverage.xml
-        env:
-          DATABASE_URL: postgresql+asyncpg://postgres:postgres@localhost:5432/utcms_rpa_test
-          REDIS_URL: redis://localhost:6379/0
-          JWT_SECRET: ${{ env.JWT_SECRET }}
-          DRIVER_ENCRYPTION_KEY: ${{ env.DRIVER_ENCRYPTION_KEY }}
 
       - name: Upload Coverage Report
         if: always()
