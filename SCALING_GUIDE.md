# راهنمای جامع مقیاس‌پذیری و توسعه ظرفیت پلتفرم (Scaling Guide)

این سند راهنمایی عملی برای برنامه‌نویسان، کارشناسان DevOps و هوش مصنوعی است تا بتوانند ظرفیت پلتفرم **BarPro** را بدون ایجاد اختلال یا تداخل، برای تعداد بیشتری مشتری، ورکر و آی‌پی توسعه دهند.

---

## فهرست مطالب
1. [افزودن مشتریان جدید (Multi-Tenancy)](#۱-افزودن-مشتریان-جدید-multi-tenancy)
2. [افزودن آی‌پی‌های جدید به سرور و تنظیم Squid](#۲-افزودن-آی‌پیهای-جدید-به-سرور-و-تنظیم-squid)
3. [توسعه تعداد ورکرها (Scaling Celery Workers)](#۳-توسعه-تعداد-ورکرها-scaling-celery-workers)
4. [استفاده از پروکسی‌های خریداری شده خارجی](#۴-استفاده-از-پروکسیهای-خریداری-شده-خارجی)
5. [ملاحظات منابع سرور و رفتارهای پیشگیرانه](#۵-ملاحظات-منابع-سرور-و-رفتارهای-پیشگیرانه)

---

## ۱. افزودن مشتریان جدید (Multi-Tenancy)

جداسازی داده‌ها (Tenant Isolation) در سطح دیتابیس توسط فیلد `client_id` در جداول اصلی برقرار است. برای ثبت مشتری جدید:

### گام اول: فراخوانی API ثبت‌نام مشتری جدید
توسط کاربر Master Admin و ارسال درخواست به مسیر زیر:
* **متد:** `POST /api/v1/auth/register` (یا از مسیر ادمین `/api/v1/admin/clients`)
* **هدر:** کوکی احراز هویت ادمین (`utcms_auth_token`) یا توکن Bearer

**نمونه بدنه درخواست (Payload):**
```json
{
  "client_code": "new_company_co",
  "name": "شرکت ترابری جدید",
  "email": "info@newcompany.com",
  "password": "SecurePassword123",
  "max_drivers": 50,
  "max_plates": 100,
  "max_concurrent_tasks": 5,
  "max_daily_tasks": 500
}
```

### گام دوم: ایزوله‌سازی کوئری‌ها در کدهای جدید
در صورتی که در آینده مدل‌های دیتابیس جدیدی اضافه کردید، همواره کوئری‌های واکشی داده‌ها را با استفاده از تابع کمکی `enforce_tenant_filter` ایزوله کنید تا هیچ کلاینتی نتواند به داده‌های کلاینت دیگر دسترسی داشته باشد:

```python
from app.auth_multitenant import enforce_tenant_filter

# نمونه کوئری ایمن
stmt = select(NewModel)
stmt = enforce_tenant_filter(current_client, stmt, NewModel)
results = await session.exec(stmt)
```

---

## ۲. توسعه ورکرها و مقیاس‌پذیری زیرساخت (Scale-Out Architecture)

در پلتفرم BarPro، دو مدل برای مقیاس‌پذیری و افزایش تعداد IPهای خروجی وجود دارد:

### مدل الف: مقیاس‌پذیری توزیع‌شده چندسروره (Model B — توپولوژی تولیدی فعلی)
در این مدل، سرور مرکزی وظیفه اجرای API، دیتابیس، ردیس و اسکجولر را بر عهده دارد و برای افزایش ظرفیت پردازش و تعداد IPهای خروجی، سرورهای VPS ریموت جدید (با سیستم‌عامل اوبونتو و IP استاتیک اختصاصی ایرانی) به ناوگان اضافه می‌شوند:

1. **باز کردن دسترسی فایروال روی سرور مرکزی:**
   ```bash
   bash scripts/add_worker_firewall.sh <WORKER_NODE_IP>
   ```
2. **راه‌اندازی ورکر نود در سرور ریموت:**
   - فایل‌های `compose/worker-node.yml` و `infra/squid/squid_worker.conf` را روی سرور ریموت کپی کنید.
   - متغیرهای `DATABASE_URL` و `REDIS_URL` را با آدرس IP سرور مرکزی مقداردهی نمایید.
   - سرویس‌ها را اجرا کنید:
   ```bash
   docker compose -f compose/worker-node.yml up -d
   ```
 3. مستندات گام‌به‌گام کامل در [docs/adding_new_worker.md](docs/adding_new_worker.md) موجود است.

### مدل ب: افزودن چند IP عمومی روی یک سرور فیزیکی (Model A)
در صورتی که چند IP عمومی روی یک سرور مرکزی ست شده باشد:
1. آی‌پی جدید را به کارت شبکه متصل کنید:
   ```bash
   sudo ip addr add [NEW_PUBLIC_IP]/24 dev eth0
   ```
2. فایل تنظیمات اختصاصی Squid جدید (`infra/squid/squid_X.conf`) بسازید و پورت آن را در `compose/proxy.yml` تعریف کنید.

---

## ۳. استخر تمیز پروکسی‌های ایرانی (Clean Iranian Proxy Pool — بدون محدودیت IP)

از نسخه **v2.9.0** به بعد، سیستم دارای یک موتور خودکار تجمیع و پایش پروکسی‌های ایرانی (`app/automation/clean_ip_pool.py`) است:
- بیش از ۱۱ منبع معتبر جهانی را پیمایش و پروکسی‌های فعال ایرانی را با پروتکل HTTPS CONNECT مستقیماً با `https://utcms.ir` اعتبارسنجی می‌کند.
- روتینگ هیبرید و چرخشی به صورت کاملاً خودکار در صورت بروز اختلال یا بلاک شدن IP ورکر، ترافیک را به بهترین پروکسی‌های استخر منتقل می‌کند (`app/automation/proxy_rotator.py`).

---

## ۴. ملاحظات منابع سرور و رفتارهای پیشگیرانه

> [!IMPORTANT]
> با ارتقای سرور مرکزی به **۱۶ گیگابایت RAM و ۴ هسته vCPU**، بودجه‌بندی منابع به شرح زیر استانداردسازی شده است:

### بودجه‌بندی RAM روی سرور مرکزی:
* **پایگاه داده PostgreSQL 16:** تخصیص حداکثر ۱.۵ گیگابایت
* **کش و صف Redis 7:** تخصیص ۵۱۲ مگابایت
* **بک‌اند FastAPI:** تخصیص ۵۱۲ مگابایت
* **فرانت‌اند Next.js 15:** تخصیص ۱ گیگابایت
* **ورکر Celery 1 سرور مرکزی:** حداکثر ۳ گیگابایت
* **سلری اسکجولر و بیت:** ۱ گیگابایت
* **فضای آزاد برای بافر سیستم‌عامل و پایش:** بیش از ۶ گیگابایت

### خطاهای متداول و راه‌حل‌ها:

| خطا / مشکل | علت احتمالی | راهکار |
| --- | --- | --- |
| `Navigation Timeout` در پلی‌رایت | پروکسی Squid قطع است یا آی‌پی مسدود شده. | سیستم به طور خودکار به Clean IP Pool سوئیچ می‌کند؛ همچنین وضعیت ورکر را در `admin/workers` بررسی کنید. |
| خطای `Out of Memory` | انباشت حافظه در مرورگر کرومیوم. | چرخه بازیافت خودکار (Browser Recycling) هر ۲۰ تسک یک‌بار اجرا می‌شود. |
| خطای بلاک آی‌پی توسط سایت هدف | ارسال درخواست‌های بیش از حد. | سیستم دارای Circuit Breaker هوشمند و تفکیک صف‌های مجزاست. |

---

*به‌روزرسانی‌شده برای نسخه v2.9.3 — 2026-08-20*
