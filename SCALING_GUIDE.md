# راهنمای جامع مقیاس‌پذیری و توسعه ظرفیت پلتفرم (Scaling Guide)

این سند راهنمایی عملی برای برنامه‌نویسان، کارشناسان DevOps و هوش مصنوعی است تا بتوانند ظرفیت پلتفرم **BarPro** را بدون ایجاد اختلال یا تداخل، برای تعداد بیشتری مشتری، ورکر و آی‌پی توسعه دهند.

---

## فهرست مطالب
1. [افزودن مشتریان جدید (Multi-Tenancy)](#۱-افزودن-مشتریان-جدید-multi-tenancy)
2. [افزودن آی‌پی‌های جدید به سرور و تنظیم Squid](#۲-افزودن-آی‌پیهای-جدید-به-سرور-و-تنظیم-squid)
3. [توسعه تعداد ورکرها (Scaling Celery Workers)](#۳-توسعه-تعداد-ورکرها-scaling-celery-workers)
4. [استفاده از پروکسی‌های خریداری شده خارجی](#۴-استفاده-از-پروکسیهای-خریداری-شده-خارجی)
5. [ملاحظات منابع سرور و رفتارهای پیشگیرانه](#۵-ملاحظات-منابع-سرور-و-رفتارهای-پیشگیرانه)

---

## ۱. افزودن مشتریان جدید (Multi-Tenancy)

جداسازی داده‌ها (Tenant Isolation) در سطح دیتابیس توسط فیلد `client_id` در جداول اصلی برقرار است. برای ثبت مشتری جدید:

### گام اول: فراخوانی API ثبت‌نام مشتری جدید
توسط کاربر Master Admin و ارسال درخواست به مسیر زیر:
* **متد:** `POST /api/v1/auth/register` (یا از مسیر ادمین `/api/v1/admin/clients`)
* **هدر:** کوکی احراز هویت ادمین (`utcms_auth_token`) یا توکن Bearer

**نمونه بدنه درخواست (Payload):**
```json
{
  "client_code": "new_company_co",
  "name": "شرکت ترابری جدید",
  "email": "info@newcompany.com",
  "password": "SecurePassword123",
  "max_drivers": 50,
  "max_plates": 100,
  "max_concurrent_tasks": 5,
  "max_daily_tasks": 500
}
```

### گام دوم: ایزوله‌سازی کوئری‌ها در کدهای جدید
در صورتی که در آینده مدل‌های دیتابیس جدیدی اضافه کردید، همواره کوئری‌های واکشی داده‌ها را با استفاده از تابع کمکی `enforce_tenant_filter` ایزوله کنید تا هیچ کلاینتی نتواند به داده‌های کلاینت دیگر دسترسی داشته باشد:

```python
from app.auth_multitenant import enforce_tenant_filter

# نمونه کوئری ایمن
stmt = select(NewModel)
stmt = enforce_tenant_filter(current_client, stmt, NewModel)
results = await session.exec(stmt)
```

---

## ۲. افزودن آی‌پی‌های جدید به سرور و تنظیم Squid

هنگامی که آی‌پی‌های عمومی (Public IPs) جدیدی را از دیتاسنتر تهیه می‌کنید، باید آن‌ها را روی کارت شبکه سیستم تنظیم کرده و سپس برای هر کدام یک درگاه پروکسی محلی با Squid تعریف کنید.

### گام اول: ست کردن آی‌پی روی سیستم‌عامل (Ubuntu)
آی‌پی جدید را به اینترفیس شبکه متصل کنید (به طور موقت یا دائمی از طریق Netplan):
```bash
sudo ip addr add [NEW_PUBLIC_IP]/24 dev eth0
```

### گام دوم: ایجاد فایل تنظیمات Squid اختصاصی
برای هر آی‌پی جدید، یک فایل پیکربندی Squid بسازید. برای مثال برای آی‌پی چهارم، فایلی به نام `infra/squid/squid_4.conf` بر اساس نمونه‌های قبلی ایجاد کنید:

```conf
# infra/squid/squid_4.conf
http_port 3131

# اتصال خروجی به آی‌پی عمومی چهارم سرور
tcp_outgoing_address [NEW_PUBLIC_IP]

# دسترسی به شبکه داخلی داکر را باز بگذارید
acl localnet src 172.16.0.0/12
http_access allow localnet
http_access allow localhost
http_access deny all
```

### گام سوم: ثبت پروکسی در Docker Compose
فایل [compose/proxy.yml](file:///Users/amirheidari/GitHub/BarPro-main/compose/proxy.yml) را ویرایش کرده و کانتینر جدید Squid را تعریف کنید:

```yaml
  squid_4:
    image: ubuntu/squid:latest
    container_name: barpro_squid_4
    restart: unless-stopped
    volumes:
      - ../infra/squid/squid_4.conf:/etc/squid/squid.conf
    # استفاده از network_mode: host برای مسیریابی مستقیم کارت‌های شبکه فیزیکی سرور
    network_mode: host
```

---

## ۳. توسعه تعداد ورکرها (Scaling Celery Workers)

پس از بالا آوردن درگاه پروکسی جدید، باید ورکر جدید Celery را ایجاد کنید تا ترافیک خود را از آن درگاه عبور دهد.

### گام اول: ویرایش فایل کامپوز بک‌اند
فایل [compose/backend.yml](file:///Users/amirheidari/GitHub/BarPro-main/compose/backend.yml) را باز کرده و سرویس ورکر جدید را تعریف کنید:

```yaml
  celery_worker_4:
    <<: *backend-common
    container_name: barpro_worker_4
    environment:
      - WORKER_ID=4
      - WORKER_4_PROXY=http://127.0.0.1:3131  # هدایت مرورگر ورکر ۴ به درگاه پروکسی ۴
    depends_on:
      - redis
      - db
    # این ورکر فقط کارهای صف عمومی را انجام می‌دهد
    command: celery -A app.workers.celery_app worker --loglevel=info -Q waybill_tasks
```

### گام دوم: اعمال تغییرات و بالا آوردن سرویس‌ها
```bash
# مجدداً کانتینرها را بیلد و راه‌اندازی کنید
bash manage.sh stop
bash manage.sh start
```

---

## ۴. استفاده از پروکسی‌های خریداری شده خارجی

اگر به جای آی‌پی سرور فیزیکی، مایل به خرید پروکسی‌های ایرانی دیتاسنتری یا خانگی (Residential) هستید:

1. خرید پروکسی به فرمت استاندارد: `http://username:password@proxy_ip:port`
2. تعریف پروکسی‌ها در متغیر محیطی `RPA_PROXIES` در فایل `.env`:
```env
RPA_PROXIES=http://user:pass@192.168.1.50:8080,http://user:pass@192.168.1.51:8080
```
3. پروکسی روتاتور سیستم به طور خودکار این آی‌پی‌ها را خوانده، تست سلامت (Ping/Geo-IP) کرده و میان ورکرها تقسیم یا چرخشی اعمال می‌کند.

---

## ۵. ملاحظات منابع سرور و رفتارهای پیشگیرانه

> [!IMPORTANT]
> افزایش تعداد ورکرها بدون در نظر گرفتن ظرفیت فیزیکی سرور، منجر به توقف کامل سیستم (Crash) خواهد شد. همواره فرمول‌های زیر را رعایت کنید.

### محاسبات رم و پردازنده (۱۲ گیگابایت رم فیزیکی فعلی):
* **هر مرورگر باز کرومیوم:** بین ۳۰۰ تا ۸۰۰ مگابایت رم مصرف می‌کند.
* **ورکرهای سلری:** در حالت اوج بار کاری تا ۲.۵ گیگابایت رم محدود شده‌اند.
* **مجموع مجاز ورکرها در سرور فعلی:** حداکثر **۳ الی ۴ ورکر فعال**.
* در صورت نیاز به تعداد کارهای موازی بیشتر از ۴ عدد، حتماً رم سرور فیزیکی را به **۱۶ یا ۲۴ گیگابایت** ارتقا دهید.

### خطاهای متداول و راه‌حل‌ها:

| خطا / مشکل | علت احتمالی | راهکار |
| --- | --- | --- |
| `Navigation Timeout` در پلی‌رایت | پروکسی Squid قطع است یا آی‌پی مسدود شده. | وضعیت اتصال پروکسی را از طریق دستور `curl -x http://localhost:port https://ident.me` تست کنید. |
| خطای `Out of Memory` | تعداد کارهای همزمان بیشتر از توان رم سرور است. | فعال‌سازی Autoscale در سلری با آپشن `--autoscale=3,1` جهت مدیریت منابع. |
| خطای بلاک آی‌پی توسط سایت هدف | ارسال درخواست‌های بیش از حد از یک آی‌پی در دقیقه. | افزایش تعداد آی‌پی‌ها یا تنظیم تاخیر شبیه‌ساز رفتار انسانی بر روی گزینه‌های طولانی‌تر. |

---

*تنظیم شده توسط ایجنت هوش مصنوعی Antigravity جهت استفاده در عملیات توسعه سیستم BarPro*
