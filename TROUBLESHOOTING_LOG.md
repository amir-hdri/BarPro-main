# 📋 گزارش جامع عیب‌یابی و رفع ریشه‌ای مشکلات — پروژه BarPro

این سند حاوی تمامی مشکلات فنی شناسایی شده در طول فرآیند توسعه و استقرار لوکال پروژه **BarPro** و نحوه برطرف کردن ریشه‌ای آن‌ها است.

---

## ۱. مشکل اتصال و خطای ۵۰۳ پروکسی‌های Squid
* **مشکل:** ورکرها هنگام شروع فرآیند استعلام سوخت یا ثبت بارنامه با خطای `HTTP 503 Service Unavailable` مواجه می‌شدند.
* **علت ریشه‌ای:** در فایل‌های کانفیگ پروکسی (`infra/squid/squid_*.conf`) دستور `tcp_outgoing_address` به IPهای سرور تولید (`188.121.123.16` و `95.38.233.90`) بایند شده بود. این IPها در محیط لوکال وجود ندارند و باعث شکست کلیه درخواست‌های خروجی Squid می‌شدند.
* **راه‌حل ریشه‌ای:**
  1. کامنت کردن خط مربوط به `tcp_outgoing_address` در فایل‌های `infra/squid/squid_1.conf` تا `squid_3.conf` در حالت لوکال.
  2. اضافه کردن کامنت راهنما جهت فعال‌سازی مجدد آن فقط در سرور تولید.
  3. ری‌استارت پروکسی‌ها با `docker compose -f compose/proxy.yml restart`.

---

## ۲. خطای رمزگشایی کلمه عبور رانندگان (`driver_password_decrypt_failed`)
* **مشکل:** هنگام بارگذاری اطلاعات رانندگان یا شروع تسک‌ها، سیستم به دلیل عدم امکان رمزگشایی رمز عبور رانندگان کرش می‌کرد یا خطا می‌داد.
* **علت ریشه‌ای:** رمزهای عبور رانندگان در دیتابیس با کلید قدیمی `DRIVER_ENCRYPTION_KEY` رمزگذاری شده بودند که با کلید جدید تولید شده در فایل `.env` همخوانی نداشت.
* **راه‌حل ریشه‌ای:**
  1. توسعه اسکریپت `scripts/reencrypt_drivers.py` که قادر است با دریافت کلید قدیمی، رمزها را رمزگشایی کرده و با کلید جدید مجدداً رمزگذاری و ذخیره کند.
  2. اضافه کردن دستور `reencrypt` به ابزار مدیریت پروژه:
     ```bash
     bash manage.sh reencrypt "OLD_KEY"
     ```
  3. تنظیم رمزهای پیش‌فرض موقت بر اساس کد ملی برای جلوگیری از کرش سیستم در محیط توسعه تا زمان وارد کردن کلمه عبور واقعی توسط کاربر.

---

## ۳. خطای تداخل کلید اصلی در دیتابیس (`duplicate key violates unique constraint`)
* **مشکل:** هنگام ثبت دستی راننده یا کلاینت جدید در پایگاه داده، خطای کلید تکراری روی فیلد `id` رخ می‌داد.
* **علت ریشه‌ای:** پس از درج‌های دستی داده‌های پشتیبان یا تست در دیتابیس، شمارنده اتوماتیک PostgreSQL (یعنی Sequence کلیدهای اصلی) با مقادیر درج شده هماهنگ نبود.
* **راه‌حل ریشه‌ای:**
  1. توسعه اسکریپت اصلاح اتوماتیک Sequenceها در `scripts/fix_db_sequences.py`.
  2. اضافه کردن دستور `fix-db` به فایل ابزار مدیریت پروژه (`manage.sh`) برای همگام‌سازی آسان:
     ```bash
     bash manage.sh fix-db
     ```

---

## ۴. خطا در ذخیره‌سازی رانندگان با ساختار `ON CONFLICT DO NOTHING`
* **مشکل:** اسکریپت‌ها یا کوئری‌های درج رانندگان بدون ایجاد خطا اجرا می‌شدند اما هیچ رکوردی در دیتابیس ثبت نمی‌شد (`INSERT 0 0`).
* **علت ریشه‌ای:** استفاده از دستور `ON CONFLICT DO NOTHING` بدون مشخص کردن فیلدهای هدف تداخل (Conflict Target) در جدول `drivers`. فیلد `utcms_username` یونیک نبوده و شرط تداخل یونیک ترکیبی `(client_id, driver_national_code)` باید به درستی تعریف می‌شد.
* **راه‌حل ریشه‌ای:**
  تصحیح کوئری درج به ساختار زیر:
  ```sql
  INSERT INTO drivers (...) VALUES (...)
  ON CONFLICT (client_id, driver_national_code) DO UPDATE
  SET ...
  ```

---

## ۵. مشکل ناسازگاری ایمیل و نام کاربری کلاینت اصلی
* **مشکل:** کاربر لوکال با ایمیل `amir@gmail.com` به دلیل تداخل فیلد یونیک `username='amir'` با کلاینت منتقل شده تولید (`hamid@gmail.com`) ساخته نمی‌شد.
* **علت ریشه‌ای:** فیلد `username` در جدول کلاینت‌ها یک قید منحصر‌به‌فرد (`UNIQUE`) دارد و نام کاربری `amir` قبلاً توسط کلاینت دیگری رزرو شده بود.
* **راه‌حل ریشه‌ای:**
  1. اصلاح نام کاربری کلاینت `hamid@gmail.com` به `hamid`.
  2. آزاد شدن نام کاربری `amir` و ساخت موفق کلاینت جدید با ایمیل `amir@gmail.com` و رمز عبور `amir123`.

---

## ۶. راه‌اندازی و اجرای امن پروژه بدون کانتینرهای قدیمی
* **اقدام:** تمامی ایمیج‌ها و کانتینرها متوقف شده و به صورت کاملاً تمیز با استفاده از فایل‌های لایه‌ای کامپوز و مدیریت شبکه مجدداً راه‌اندازی شدند تا از تداخل نسخه‌های قدیمی در کش داکر جلوگیری شود.
